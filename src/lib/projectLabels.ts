import type { ProjectStatus } from '../types'

export const PROJECT_STATUS_FR: Record<ProjectStatus, string> = {
  proposition: 'Proposition',
  simulation: 'Simulation',
  souscription: 'Souscription',
  documents: 'Justificatifs',
  envoi: 'Envoyé',
}
